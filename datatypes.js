"use strict"//treat all js code as newer version

//alert(2+2)//used on browser,but we are on node

let num=1;//int,bigint
let name="amaan"//string
let isValid=true//boolean


//then there is
//null - standalone value
//symbol - for uniqueness
//undefined - when no value is assigned

// console.log(typeof num)
console.log([typeof num,typeof name,typeof isValid,typeof null,typeof undefined])
